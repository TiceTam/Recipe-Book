{
  "name": "recipe-creator",
  "version": "1.0.0",
  "description": "server side",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:digitalocean": "npm install && cd client && npm install && npm run build && cd ../ && node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^8.0.0",
    "path": "^0.12.7"
  }
}
